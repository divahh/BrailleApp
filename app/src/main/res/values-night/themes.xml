<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BrailleApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- For Button and Appbar. -->
        <item name="colorPrimary">@color/dark_blue</item>
        <!-- For status bar. -->
        <item name="colorPrimaryVariant">@color/black</item>
        <!-- For text or icon on colorPrimary. -->
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary button. -->
        <item name="colorSecondary">@color/dark_blue</item>
        <!-- hover button. -->
        <item name="colorSecondaryVariant">@color/black</item>
        <!-- text or icon on colorSecondary. -->
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize font. -->
        <item name="android:fontFamily">@font/plusjakartasans_extrabold</item>
        <!-- Customize navigation bar. -->
        <item name="android:navigationBarColor">@color/black</item>
    </style>
</resources>